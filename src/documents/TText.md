# 🧩 Component: `TText`

## Tổng quan
Văn bản với màu/kích thước theo theme hoặc tuỳ chỉnh.

---

## ⚙️ Thuộc tính (`props`)

| Tên | Kiểu | Mặc định |
|-----|------|----------|
| `color` | `any` | `"var(--t-color-text)"` |
| `size` | `any` | `"standard"` (small/standard/large hoặc CSS size) |

---

## Cách dùng
```vue
<t-text size="small" color="secondary">Helper</t-text>
```
